-- Content (problems, examples) migrated from D1 to R2 bundles.
-- These tables are no longer needed — content is served from R2.
-- review_log.assessment_content_id remains as a logical reference to R2 problem IDs.

DROP TABLE IF EXISTS `assessment_content`;--> statement-breakpoint
DROP TABLE IF EXISTS `instructional_content`;
