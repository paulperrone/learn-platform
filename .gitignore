node_modules/
dist/
.wrangler/
.dev.vars
*.local
.DS_Store
# Content lives in separate learn-content repo (top-level only)
/content/
tools/visualize-graph.html
audit/learner-simulations/runs/
# All audit reports are generated artifacts (just audit, just evaluate, /content-review, etc.)
audit/reports/*.json
audit/reports/*.md
audit/reports/*.html
audit/reports/snapshots/
audit/reports/healing/
audit/reports/training/latest-state.json
audit/reports/content-reviews/

# Workflow
CLAUDE.local.md
.workflow/*.local
.workflow/*.local.*
.workflow/memory/*.mv2
.workflow/task/

# Provider symlinks (regenerable)
.claude
CLAUDE.md
